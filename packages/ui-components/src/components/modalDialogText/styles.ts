import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    drawer: {
        flexShrink: 0
    },
    notificationsModal: {
        width: 360
    },
    modalTitle: {
        paddingTop: 28,
        paddingBottom: 8
    },
    modalBodyText: {
        color: "#222222",
        fontSize: 14,
        whiteSpace: "pre-wrap"
    }
});

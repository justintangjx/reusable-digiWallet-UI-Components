import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
    popupSize: {
        height: 247,
        width: 326
    },
    spinnerContainer: {
        marginTop: 53,
        marginLeft: 140,
    },
    colorPrimary: {
        color: "#1565C0",
    },
});
